package com.app.note_lass.module.student.data.handbook

import java.util.HashMap

data class HandBookSubmitState(
    val isLoading : Boolean = false,
    val isSuccess : Boolean = false,
    val isError : Boolean = false
)
